{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "npm-run-all -p start:db start:produtos start:produto-cosif",
    "start:db": "json-server --watch db.json --port 3000",
    "start:produtos": "json-server --watch produtos.json --port 3001",
    "start:produto-cosif": "json-server --watch produto_cosif.json --port 3002"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "^1.0.0-alpha.23"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
